repos:
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.1.0
    hooks:
      - id: prettier
  - repo: local
    hooks:
      - id: eslint-local
        name: eslint (local)
        entry: npx eslint
        pass_filenames: true
        language: system
